Build dependencies
==================
Translation catalogs are external runtime files. Restore quiterss_*.ts into
lang/ before running qmake to build them with the matching Qt lrelease tool.
Precompiled .qm files can also be installed or added by users without rebuilding.
See lang/README.md for discovery paths, optional metadata and installation.

  C++17-capable GCC or Clang and make (Windows recipe: MSYS2 UCRT64 GCC)
  SQLite 3.30.1 or newer, including development headers and sqlite3.pc
  libxml2 development headers and libxml-2.0.pc
  pkg-config (pkgconf is supported)
  Qt 5.15.x (default) or Qt 6.2+ development packages and matching qmake
  Qt modules: Core, Gui, Widgets, PrintSupport, Network, Sql, Xml
  Qt's QSQLITE plugin (used for initialization and VACUUM)
  Qt 6 additionally requires Core5Compat for the existing QTextCodec decoders
  QtSingleApplication for the selected Qt: headers, library and qtsingleapplication.prf
  miniaudio 0.11.25 headers (explicit preparation helper below; no shared library)

Selecting Qt 5 or Qt 6
=====================
The application defaults to Qt 5. Invoke qmake from the matching Qt installation:

  qmake-qt5 ../QuiteRSS.pro CONFIG+=release CONFIG-=debug_and_release
  qmake6 ../QuiteRSS.pro QUITERSS_QT_MAJOR=6 CONFIG+=release CONFIG-=debug_and_release

QUITERSS_QT_MAJOR accepts only 5 or 6; a mismatch with qmake's Qt is an error.
It cannot switch the libraries used by an already selected qmake executable.
Use separate application build directories and QtSingleApplication work directories
for each Qt/toolchain. Prepare the dependency with the SAME qmake as the application.
The platform recipes below retain Qt 5 defaults. Only Windows CI selects Qt 6.

For Qt 6 on MSYS2, substitute qt6-base, qt6-tools and
qt6-imageformats for the qt5 packages below, and add
mingw-w64-ucrt-x86_64-qt6-5compat. Pass --qmake qmake6 to the preparation helper,
then use the Qt 6 application command above. Deploy with windeployqt6;
the Qt 5 --no-angle option is not applicable.

On Linux, install the equivalent Qt 6 development packages, including
Core5Compat, and use the helper if no matching QtSingleApplication feature
is packaged. On Homebrew, select the Qt 6 formula's bin directory instead of
qt@5, prepare QtSingleApplication with that qmake, and pass QUITERSS_QT_MAJOR=6.
Executable names vary by distribution; an absolute path to qmake is also valid.

Both builds read/write UTF-8 INI values so non-ASCII settings survive switching
between them. Existing Qt 5-generated INI files remain readable. To return to
an older, unpatched Qt 5 build after running Qt 6, use a settings backup: older
builds without an explicit UTF-8 INI codec may misread non-ASCII values.

Notification audio
==================
Qt Multimedia is not used. Miniaudio provides native output through WASAPI on
Windows, Core Audio on macOS, and PulseAudio/ALSA on Linux. PipeWire systems can
use their PulseAudio-compatible server. A working audio service/device and the
corresponding runtime client library are still required; codec plugins are not.
The null (silent) backend is disabled so output failures are reported.

The explicit helper downloads only miniaudio.h and LICENSE from upstream commit
9634bedb5b5a2ca38c1ee7108a9358a4e233f14d (0.11.25), verifies their SHA-256 hashes,
and installs them outside the source tree. It does not build or install packages:

  python3 scripts/prepare-miniaudio.py --prefix "$HOME/quitersslean-miniaudio"
  export MINIAUDIO_INCLUDE_DIR="$HOME/quitersslean-miniaudio/include"

For an offline preparation, add --source-dir /path/to/miniaudio-0.11.25 with
the upstream miniaudio.h and LICENSE files; the same hashes are checked.
Alternatively, point MINIAUDIO_INCLUDE_DIR at compatible 0.11.25+ headers from
the 0.11.x series. Version 0.12 requires a separate integration review.
The header implementation is compiled once in SoundPlayer; do not also link
a miniaudio library. There are no downloads during application qmake/make.
On Windows use a native C:/... include path, as in the recipe below.

The normal WAV sound picker and saved sound paths are unchanged. Built-in
WAV/FLAC/MP3 decoding is available; manually configured AAC, Ogg/Vorbis or other
files formerly supported by a Qt backend are not supported. The legacy hidden
Settings/useMediaPlayer key is ignored; all sounds use the same FIFO queue,
including configurations that previously launched an external "play" command.
Errors are logged with the filename and backend result. Repeated failures are
suppressed in the UI until playback starts successfully again.

Audio initialization and file loading run off the UI thread. The output device
and miniaudio threads are released after an idle tail, and worker teardown is
joined on application destruction. Closing to the tray does not stop the player.
Linux links dl/pthread/m; macOS links the system CoreFoundation/CoreAudio/
AudioToolbox frameworks directly. Miniaudio adds no runtime DLL to deploy on
Windows. Its MIT-0/Unlicense text and source provenance are installed by the
helper under share/licenses/miniaudio; include these in distribution notices.

Article age retention
=====================
With shutdown cleanup and its maximum-age option enabled, ordinary RSS/Atom
updates ignore entries older than the publication-age cutoff. The first
successful fetch of a new feed is exempt; emptying an existing feed is not.
On shutdown, expired dated articles are physically removed, including old
duplicate records. Existing unread/starred/labeled protections still apply
to articles that have not already been cleaned up.

Count-based cleanup keeps duplicate records until they expire by age. Entries
without a usable publisher date retain receipt-age cleanup and duplicate
records. Legacy synthetic receipt timestamps are treated conservatively as
undated. The manual cleanup wizard retains its independent receipt-age rules.
Changing the age window later cannot restore already removed articles, but a
subsequent feed update can import entries allowed by the new window.

Article sharing data
====================
The installed application reads social-networks.ini from its resource directory
and icons from the adjacent social-networks directory. On Linux with PREFIX=/usr,
these are /usr/share/quiterss/social-networks.ini and
/usr/share/quiterss/social-networks/. The qmake install target installs both.

Only when the installed INI file does not exist, the application looks beside
the user's QuiteRSS INI file for social-networks.ini and social-networks/. The
user definition is then used in full; installed and user definitions are never
merged. Startup diagnostics use the [article-sharing] prefix on stderr.

The INI [services] order value lists service section names. Each section accepts
name, icon, url, enabled and an optional action name used to retain shortcut and
toolbar compatibility. URL templates support the percent-encoded placeholders
{url} and {title}. Icons must be plain filenames within social-networks/.

SQLite and libxml2 are required external libraries on EVERY platform. qmake
uses pkg-config for include paths, library paths and transitive link flags.
Set PKG_CONFIG_PATH to the directories containing sqlite3.pc and libxml-2.0.pc
for a non-default installation. On Windows use native paths with forward
slashes (C:/...) and the native UCRT64 pkg-config, not /usr/bin/pkg-config.
Do not mix architectures, MSVC/MinGW, or MSYS2 environments in one build.

SQLitex in 3rdparty/sqlitex remains the application's custom Qt SQL driver.
It and the direct SQLite backup calls link to the same external SQLite engine.
Do not substitute Qt's QSQLITE plugin for SQLitex. Use a normal thread-safe
SQLite build with UTF-16, declared column types and the backup API enabled.
The old SQLITE_OMIT_* defines applied only to the deleted engine sources;
application compiler flags cannot configure a prebuilt SQLite library.

QtSingleApplication must be installed before application qmake runs. Fedora
supplies the library AND qmake feature. Where a suitable package is unavailable,
the explicit helper below builds a static QtSingleApplication library against
your installed shared Qt and installs headers and qtsingleapplication.prf.
It fetches only qtproject/qt-solutions commit:
  777e95ba69952f11eaec0adfb0cb987fabcdecb3
Sources, temporary build files and installed outputs stay under --work-dir,
which must be OUTSIDE the application source tree. The helper requires Python 3
and Git. It is never invoked by application qmake/make and performs no package
installation. A successful repeated run reuses the verified source checkout;
a partial/modified checkout requires a fresh work directory.

Use a separate work directory for each compiler/Qt installation/architecture.
The helper defaults to release; pass --config debug to prepare the debug archive
as well. Request only configurations supplied by your Qt package. Rebuild both
archives after changing the toolchain. QMAKEFEATURES must point at the installed
features directory, not the upstream .pri file. A .pri that adds source files
is not an installed-library feature. SYSTEMQTSA is no longer needed.

Linux (Fedora 44)
================
  sudo dnf install python3 gcc-c++ make pkgconf-pkg-config qt5-qtbase-devel \
    qt5-qttools-devel qtsingleapplication-qt5-devel \
    sqlite-devel libxml2-devel qt5-qtimageformats
  python3 scripts/prepare-miniaudio.py --prefix "$HOME/quitersslean-miniaudio"
  export MINIAUDIO_INCLUDE_DIR="$HOME/quitersslean-miniaudio/include"
  mkdir _build
  cd _build
  qmake-qt5 ../QuiteRSS.pro CONFIG+=release CONFIG-=debug_and_release PREFIX=/usr
  make -j2
  sudo make install

Other Linux distributions need the equivalent development packages and Qt 5
QtSingleApplication feature. qmake may be named qmake or qmake-qt5. For Clang on Linux,
install clang and lld, then pass -spec linux-clang and QMAKE_LFLAGS+=-fuse-ld=lld
to qmake so compiler-specific LTO flags match the compiler and linker.

Windows (MSYS2 UCRT64, x86-64)
=============================
Install MSYS2 from https://www.msys2.org/ and fully update it with pacman -Syu
(follow its restart instructions). Use the UCRT64 shell for ALL commands below.
The recipe uses package-managed Qt, SQLite, libxml2 and their runtime libraries;
it does not use the Qt online installer's separate MinGW or an MSVC toolchain.

  pacman -S --needed git \
    mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-make \
    mingw-w64-ucrt-x86_64-python mingw-w64-ucrt-x86_64-pkgconf \
    mingw-w64-ucrt-x86_64-qt5-base mingw-w64-ucrt-x86_64-qt5-tools \
    mingw-w64-ucrt-x86_64-qt5-imageformats \
    mingw-w64-ucrt-x86_64-sqlite3 mingw-w64-ucrt-x86_64-libxml2 \
    mingw-w64-ucrt-x86_64-openssl

From the application source directory:
  export PKG_CONFIG_PATH="$(cygpath -m /ucrt64/lib/pkgconfig)"
  dep_work="$(cygpath -m "$HOME/quitersslean-deps-ucrt64")"
  /ucrt64/bin/python scripts/prepare-qtsingleapplication.py \
    --work-dir "$dep_work" --qmake qmake-qt5 --make mingw32-make
  export QMAKEFEATURES="$dep_work/install/features"
  python3 scripts/prepare-miniaudio.py --prefix "$dep_work/miniaudio"
  export MINIAUDIO_INCLUDE_DIR="$dep_work/miniaudio/include"
  mkdir _build
  cd _build
  qmake-qt5 ../QuiteRSS.pro CONFIG+=release CONFIG-=debug_and_release
  mingw32-make -j2
  mingw32-make install
  ./release/target/QuiteRSS.exe

The install target copies social-networks.ini and social-networks/ beside the
executable. The UCRT64 shell supplies the dependency DLL paths for local
testing. MSVC is not covered by this recipe or CI: it needs compatible external
libraries and its own installed qtsingleapplication.prf; do not link the UCRT64
C++ archive into an MSVC application.

macOS (Homebrew, native architecture)
====================================
Install Xcode Command Line Tools and Homebrew (https://brew.sh/) first.
  brew install qt@5 sqlite libxml2 pkgconf python

From the application source directory:
  export PATH="$(brew --prefix qt@5)/bin:$PATH"
  export PKG_CONFIG_PATH="$(brew --prefix sqlite)/lib/pkgconfig:$(brew --prefix libxml2)/lib/pkgconfig"
  dep_work="$HOME/quitersslean-deps-macos"
  python3 scripts/prepare-qtsingleapplication.py --work-dir "$dep_work"
  export QMAKEFEATURES="$dep_work/install/features"
  python3 scripts/prepare-miniaudio.py --prefix "$dep_work/miniaudio"
  export MINIAUDIO_INCLUDE_DIR="$dep_work/miniaudio/include"
  mkdir _build
  cd _build
  qmake ../QuiteRSS.pro CONFIG+=release CONFIG-=debug_and_release
  make -j2
  open release/target/QuiteRSS.app

This default recipe uses qt@5; see the Qt 6 opt-in instructions above.
The explicit pkg-config paths select
Homebrew SQLite/libxml2 rather than SDK libraries. Use one native Homebrew
architecture consistently; this recipe does not produce a universal binary.

Configuration and deployment notes
==================================
If qmake reports a missing dependency, verify in the SAME build environment:
  qmake -query QT_VERSION
  pkg-config --modversion sqlite3 libxml-2.0
  pkg-config --cflags --libs sqlite3 libxml-2.0
  pkg-config --atleast-version=3.30.1 sqlite3
Also check QMAKEFEATURES and that its qtsingleapplication.prf refers to installed
headers/libraries. An existing packaged feature may be used instead of the
helper. Match the Qt version, compiler, architecture and debug/release ABI.
No application configuration/build step downloads dependencies.

These recipes and CI compile for local use; they do not create self-contained
release packages. For redistribution, use the matching windeployqt/macdeployqt
on release/target/QuiteRSS.exe or release/target/QuiteRSS.app, then inspect and
deploy non-Qt dependencies as well (SQLite, libxml2 and their dependencies,
OpenSSL/TLS backend, compiler runtime where needed). Deployment tools alone
must not be assumed to collect every non-Qt library. macOS bundles need their
non-system dylib paths fixed and signing performed after all files are copied.
Include the licenses of deployed dependencies, including the helper's installed
share/licenses/qtsingleapplication directory. Packaged shared QtSingleApplication
builds also require that shared library; the helper's static build does not.

Keep the QSQLITE plugin, Qt platform plugin and image-format
plugins, including WebP. Linux packages should depend on the selected Qt's
image-format package (qt5-qtimageformats for Qt 5 on Fedora), or its distribution
equivalent at runtime. Without WebP support the reader does
not advertise WebP but cannot display images available only in that format.
Provide the matching TLS runtime and a usable certificate trust store; verify
HTTPS feeds and article images on the target machine. Do not disable certificate
verification to work around deployment mistakes.

After a build, check database creation/reopening, in-memory database save/restore,
Unicode sorting and REGEXP, a second-instance launch, HTTPS feeds and images,
notifications and printing/export. Windows/macOS builds still
need these runtime checks; compilation alone cannot establish them.
